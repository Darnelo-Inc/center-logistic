# center-logistic

To browse the project:
1. Download zip project
2. Download gulp if you don't have it yet
3. In the project folder in git, run the "gulp i" command (install modules) 
4. In the project folder in git, run the "gulp" command (run project)
